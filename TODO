* For now it is too specific - only for pacman. Think of defining some interface, so modules for other package managers can be used as well (emerge, apt, etc...).
* How to get download/install size of individual packages?
